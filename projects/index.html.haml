---
layout: page
title: Projects
---
#projects.container.projects
  .row
    - page.projects.each do |project|
      .col-md-6.col-lg-4.project{:id => "#{File.basename(project.relative_source_path, '.textile')}" }
        .content
          .summary
            %h2= project.title
            .date
              .fa.fa-clock-o.fa-fw
              = project.project_date
            .technos
              .fa.fa-cogs.fa-fw
              = project.project_technologies.join(", ")
          %div{:class=> "detail#{1 > 2 && project.has_more ? ' has-more' : ''}"}
            .description
              = project.project_description
            - if 1 > 2 && project.has_more
              .link-more
                %a.alt.button{:href=> project.url}
                  .fa.fa-plus-circle.fa-lg.fa-fw
                  Learn more

