---
layout: page
title: Projects
---
#projects.container.projects
  .row
    - page.projects.each do |project|
      .col-sm-6.col-lg-4.project{:id => "#{File.basename(project.relative_source_path, '.textile')}" }
        .content
          .summary
            %h2= project.title
            .date
              .fa.fa-clock-o.fa-fw
              = project.project_date
            .technos
              .fa.fa-cogs.fa-fw
              = project.project_technologies.join(", ")
          %div{:class=> "detail#{project.has_more ? ' has-more' : ''}"}
            .description
              = project.project_description
            - if project.has_more
              .link-more
                %a.alt.button{:href=> project.url}
                  .fa.fa-cogs.fa-fw
                  Learn more

